\section{Vectors and Vector Spaces}
\subsection{Motivation} % Why do we need vectors? Two example problems: (1) bouncing off a wall in a 3D computer games, (2) Newtonian gravity force in coordinate form
\begin{motivation}{Gravity Force}{gravity_force}
	Consider a simple simulation of classical (\enquote{Newtionian}) gravity, say of a planet with mass $m$ orbiting a star with mass $M$ (where $M\gg m$). At any given moment, the force acting on the planet due to the star's gravity has magnitude
	\begin{equation}
		F = G\frac{Mm}{r^{2}},
		\label{eq:gravity_force}
	\end{equation}
	where $r$ is the distance between the planet and the star at that moment, and $G$ is a universal constant (specifically, $G=\SI{6.674e-11}{\newton\meter\squared\per\kilo\gram\squared}$).

	\vspace{1em}
	However, since we live in 3-dimensional space\footnote{allegedly.}, we also want this force to be pointing in the direction from the planet to the star. To do this, we need to understant how to describe orientations in 3-dimensional space, and be able to pack together both orientation and magnitude in the correct way.
\end{motivation}

\begin{motivation}{Bouncing/Reflecting Off a Wall}{wall_bounce}
	Imagine we are writing a 3D computer game in which some object is bouncing off a wall. Alternatively, consider 3D graphics in general, where one simulates rays of light reflecting off of smooth surfaces. In both cases the same problem needs to be solved: given a flat plane of some orientation in space, and an object hitting it from some direction - what would be the direction of the object after it bounces off the plane?

	\vspace{1em}
	\begin{center}
		\tdplotsetmaincoords{75}{30}
		\begin{tikzpicture}[tdplot_main_coords, rotate=35]
			\begin{scope}[canvas is xy plane at z=0]
				\draw[xpurple, thick, fill=xpurple, opacity=0.2] (-3,-3) -- (-3,3) -- (3,3) -- (3,-3) -- cycle;
				\draw[help lines, opacity=0.25] (-3,-3) grid (3,3);
			\end{scope}
			\draw[vector, xred, dashed] (1,1,2) -- (0,0,0);
			\draw[vector, xblue, dashed] (0,0,0) -- (-1,-1,2);
		\end{tikzpicture}
	\end{center}

	\vspace{1em}
	To solve this problem we will need to know how to represent quantities like position, velocity and orientation in 3D, and more generally points, lines and planes - and to know how to calculate the intersection between these different objects. By the end of this section you should be able to solve this problem by yourself, and even generalize it to any number of dimensions.
\end{motivation}

\lipsum[1]

\subsection{Geomteric Properties of Vectors} % Definition, operations (addition, scaling)

Text text text\dots (what are vectors, pictorial representation, notation, all starting from the origin, etc.)

Vectors have two important properties: they can be added together and be scaled to form other vectors.
\begin{descitemize}
	\item[Adding vectors together] any two vectors of the same dimensionality (e.g. 2-dimensional, 3-dimensional, 4-dimensional, etc.) can be added together, and the result is a third vector. Pictorially, here are two vectors $\vecA$ and $\vecB$ which we want to sum together:

	\begin{center}
		\begin{tikzpicture}[fig with vectors]
			\pic{VecSum};
			\draw[vector={xred}] (o) -- (a) node[left, xshift=-5pt] {$\vecA$};
			\draw[vector={xblue}] (o) -- (b) node[below, yshift=-3pt] {$\vecB$};
		\end{tikzpicture}
	\end{center}

	The steps to produce the sum of $\vecA$ and $\vecB$ is the following:
	\begin{enumerate}
		\item move $\vecB$ such that its origin aligns with the head of $\vecA$:
		      \begin{center}
			      \begin{tikzpicture}[fig with vectors]
				      \draw[vector={xred}] (o) -- (a) node[left, xshift=-5pt] {$\vecA$};
				      \foreach \op in {0.2, 0.4, ..., 0.8}{
						      \draw[dashedvector={xblue}, dashed, opacity=\op] ($(o)!\op!(a)$) -- ++(b);
					      }
				      \draw[vector={xblue}] (a) -- ++(b) node[right] {$\vecB$};
			      \end{tikzpicture}
		      \end{center}
		\item draw a new vector from the origin to the head of the shifted $\vecB$:
		      \begin{center}
			      \begin{tikzpicture}[fig with vectors]
				      \draw[vector={xred}] (o) -- (a) node[left, xshift=-5pt] {$\vecA$};
				      \draw[vector={xblue}] (a) -- ++(b) node[above left] {$\vecB$};
				      \draw[vector={xorange}] (o) -- (c) node[right] {\color{black}$\vecC=\vecA+\vecB$};
			      \end{tikzpicture}
		      \end{center}
		\item the resulting vector $\vecC$ \textit{is} the sum of $\vecA$ and $\vecB$. Here are all three vectors together from the same origin:
		      \begin{center}
			      \begin{tikzpicture}[fig with vectors]
				      \draw[vector={xred}] (o) -- (a) node[left, xshift=-5pt] {$\vecA$};
				      \draw[vector={xblue}] (o) -- (b) node[above left] {$\vecB$};
				      \draw[vector={xorange}] (o) -- (c) node[right] {\color{black}$\vecC=\vecA+\vecB$};
			      \end{tikzpicture}
		      \end{center}

		      This method is called the \newterm{triangle method}, and it displays an important property of vector addition: it is commutative! This means that the order of addition doesn't matter: $\vecA+\vecB=\vecB+\vecA$. To see this visually, we can apply the triangle method twice, i.e. moving both $\vecA$ \textit{and} $\vecB$:
		      \begin{center}
			      \begin{tikzpicture}[fig with vectors]
				      \draw[vector, xred] (o) -- (a) node[left, xshift=-5pt] {$\vecA$};
				      \draw[vector, xblue] (o) -- (b) node[below] {$\vecB$};
				      \draw[vector, xorange, opacity=0.25] (o) -- ($(a)+(b)$) node[right] {\color{black}$\vecC=\vecA+\vecB$};
				      \draw[dashedvector={xblue}] (a) -- ++(b);
				      \draw[dashedvector={xred}] (b) -- ++(a);
			      \end{tikzpicture}
		      \end{center}

		      We see that no matter which vector we add onto which (i.e. what is the order of addition) - the result is the same. The shape formed by this demonstration is called a \newterm{parallelogram}, and it is incredibly important in linear algebra as a whole as we will see in future sections.
	\end{enumerate}

	This method of summing vectors can be applied to any number of vectors. For example, consider the following 5 vectors:
	\begin{center}
		\begin{tikzpicture}[fig with vectors]
			\coordinate (a) at (0.5,1);
			\coordinate (b) at (-1,0.75);
			\coordinate (c) at (1.5,0.5);
			\coordinate (d) at (0,-0.65);
			\coordinate (e) at (1,0);
			\coordinate (base) at (o);
			\foreach [count=\i] \coord in {a, b, c, d, e}{
					\draw[vector={xcol\i}] (o) -- (\coord);
				}
		\end{tikzpicture}
	\end{center}
	To sum them together, all we need is to start with one the vectors (say, the red one) and use the triangle method to add the other vectors step by step:
	\begin{center}
		\begin{tikzpicture}[fig with vectors]
			\coordinate (base) at (o);
			\foreach \coord in {a, b, c, d, e}{
					\draw[vector={x\coord col}] (base) -- ++(\coord);
					\coordinate (base) at ($(base)+(\coord)$);
				}
			% \draw[dashedvector={gray}] ($(o)+(xsh)$) -- ($(base)+(xsh)$);
		\end{tikzpicture}
	\end{center}
	Finally, we connect the origin of the first vector with the head of the last vector (in this case the orange one):
	\begin{center}
		\begin{tikzpicture}[fig with vectors]
			\coordinate (base) at (o);
			\foreach [count=\i] \coord in {a, b, c, d, e}{
					\draw[vector={x\coord col}] (base) -- ++(\coord);
					\coordinate (base) at ($(base)+(\coord)$);
				}
			\draw[dashedvector={gray}] (o) -- (base);
		\end{tikzpicture}
	\end{center}
	and that's it, the dashed gray vector is the sum of all five vectors. To see that the order of addition really doesn't matter we can do this again but this time starting with the green vector and changing the summation order completely:
	\begin{center}
		\begin{tikzpicture}[fig with vectors]
			\coordinate (base) at (o);
			\foreach [count=\i] \coord in {c, e, a, b, d}{
					\draw[vector={x\coord col}] (base) -- ++(\coord);
					\coordinate (base) at ($(base)+(\coord)$);
				}
			\draw[dashedvector={gray}] (o) -- (base);
		\end{tikzpicture}
	\end{center}
	As can be seen, the resulting dashed gray vector is exactly the same as before.

	Another important property of the addition of vectors is that we can always take a vector and rotate it by $\ang{180}$, giving us a vector that \enquote{cancels} the original vector by summation. For example, the following red vector is rotated to yield its opposite, colored in orange:
	\begin{center}
		\begin{tikzpicture}[fig with vectors]
			\draw[vector={xred}] (o) -- (a);
			\draw[vector={xorange}] (o) -- ($-1*(a)$);
		\end{tikzpicture}
	\end{center}
	These vectors added together using the triangle method yield a vector with $0$ norm:
	\begin{center}
		\begin{tikzpicture}[fig with vectors]
			\draw[vector={xred}] (o) -- (a);
			\coordinate (shift) at (2pt, -2pt);
			\draw[vector={xorange}] ($(a)+(shift)$) -- (shift);
		\end{tikzpicture}
	\end{center}
	(here I drew the vectors a bit shifted from each other so that we can see both)

	Such a vector is called the \newterm{zero vector}, denoted as $\vec{0}$. It has no direction, and a norm of $0$. Adding $\vec{0}$ to any other vector is the same as not adding any vector at all (and it doesn't matter \enquote{from which side} we add $\vec{0}$):
	\begin{equation}
		\vec{a} + \vec{0} = \vec{0} + \vec{a} = \vec{a}.
		\label{eq:addition with the zero vector}
	\end{equation}
	We say that the zero vector is \textit{agnostic} (also: neutral) to addition.

	\item[Scaling vectors] any vector can be \newterm{scaled} by any real number, which in this context we call a \newterm{scalar}. Scaling by a number simply \enquote{stretches} the vector by the number. For example, in the following figure a red vector is scaled by a scalar $\lambda$, taking on different values:

	\newcommand{\testest}[1]{
		\tikz[fig with vectors]{
			\draw[opacity=0] (-0.5,-2) rectangle (0.5,2);
			\ifthenelse{\equal{#1}{0}}{}{
				\draw[vector={xred}] (o) -- ($#1*(a)$);
			}
		}
	}
	\begin{center}
		\begin{tabular}{ccccccc}
			$\lambda\ =$ & $1$         & $2$         & $0$         & $\frac{1}{2}$ & $-1$         & $-\frac{3}{2}$ \\
			             & \testest{1} & \testest{2} & \testest{0} & \testest{0.5} & \testest{-1} & \testest{-1.5} \\
		\end{tabular}
	\end{center}

	This example shows us several things:
	\begin{itemize}
		\item no matter by which positive scalar we scale a vector, it always points in the same direction.
		\item If we scale a vector by $0$ it becomes the zero vector.
		\item Finally, scaling a vector by a negative scalar rotates the vector by $\ang{180}$ and then scales it by the absolute value of the scalar.
	\end{itemize}

	This is similar to how real numbers behave under multiplication: multiplying a real number by a negative number is, in a sense, \enquote{flipping} and scaling it relative to $0$ (the origin). For example, multiplying $3$ by $-2$ flips the the number to point to the left, and then scales it to be of length $6$:

	\begin{center}
		\begin{tikzpicture}
			\draw[thick] (-4,0) -- (4,0);
			\draw[thick, dotted] (-4,0) -- (-4.5,0) node[left] {$-\infty$};
			\draw[thick, dotted] (4,0) -- (4.5,0) node[right] {$+\infty$};
			\foreach \x in {-3.5, -3, ..., 3.5}{
					\draw[thick] (\x,-0.1) -- (\x,0.1);
				}
			\node (A) at (0,-0.4) {$0$};
			\node at (1.5,-0.4) {$3$};
			\node at (-3,-0.4) {$-6$};
			\draw[vector={xred}] (0,0.25) -- ++(1.5,0) node[above, xshift=-3mm] (B) {};
			\draw[vector={xblue}] (0,0.25) -- ++(-3,0) node[above, xshift=-1.5+3mm] (C) {};
			\draw[
				stealth-, thick, dashed,
				postaction={decorate, decoration={raise=1.5ex, text along path, text align=center, text={|\sffamily|multiply by (-2)}}}
			] (C) to [bend left=45] (B);
			\fill (0,0.25) circle (0.05);
		\end{tikzpicture}
	\end{center}

	Therefore, scaling a vector is denoted just like multiplication of real numbers: $\lambda\vec{a}$, where $\lambda$ is the scalar. In the above example, if the red vector is $\vecA$, then the other five vectors can be written as $2\vecA,\ 0\vecA,\ \frac{1}{2}\vecA,\ -1\vecA$ and $-\frac{3}{2}\vecA$.

	Furthermore, we can place the vector notation in different places and even hiding the scalar depending on convinience, so for example we can write $-\vecA$ instead of $-1\vecA$ and $-\frac{3\vecA}{2}$ instead of $-\frac{3}{2}\vecA$.

	Just like with real numbers, we call $-\vecA$ the \newterm{additive inverse} of $\vecA$.
\end{descitemize}

In fact, the similarities to real number don't end there: subtracting a vector $\vecB$ from another vector $\vecA$ is simply adding $\vecA$ and the additive inverse of $\vecB$:
\begin{equation}
	\vecA - \vecB = \vecA + (-\vecB).
	\label{eq:subtracting vectors}
\end{equation}
Visually, if we place the origin of $\vecA-\vecB$ at the head of $\vecB$, its head would be at the same point as the head of $\vecA$:
\begin{center}
	\begin{tikzpicture}[fig with vectors]
		\coordinate (a) at (-1.5,2.5);
		\coordinate (b) at (2.5,2);
		\draw[vector={xred}] (o) -- (a) node[pos=1.1] {$\vecA$};
		\draw[vector={xblue}] (o) -- (b) node[pos=1.1] {$\vecB$};
		\draw[vector={xorange}] (b) -- (a) node[midway, above, rotate=-7] {\color{black}$\vecC=\vecA-\vecB$};
	\end{tikzpicture}
\end{center}

While subtration is not commutative, it is \newterm{antisymmetric}:
\begin{equation}
	\begin{aligned}
		\vecA - \vecB = -\left(-\vecA + \vecB\right) = -\left(\vecB + \left(-\vecA\right)\right) = -\left(\vecB - \vecA\right),
	\end{aligned}
	\label{eq:anti symmetry of vec subtraction}
\end{equation}
i.e. flipping the order of subtraction changes the sign of the expression.

% \definecolor{starcol}{HTML}{FF8161}
% \definecolor{planetcol}{HTML}{7161EF}
% \definecolor{ptoscol}{HTML}{00B289}
\colorlet{starcol}{xred}
\colorlet{planetcol}{xblue!80!xgreen}
\colorlet{ptoscol}{orange!75!xred}
\newcommand{\vecSt}{\textcolor{starcol}{\vec{r}_{s}}}
\newcommand{\vecPl}{\textcolor{planetcol}{\vec{r}_{p}}}
\newcommand{\vecPtoS}{\textcolor{ptoscol}{\vec{r}_{p\to s}}}
\newcommand{\uvecPtoS}{\textcolor{ptoscol}{\uvec{r}_{p\to s}}}

So we can use vector subtraction as a convinient way to point from one vector to another. This can be utilized to implement the gravity force in \autoref{motivation:gravity_force}: if we know the position of the planet (let's call it $\vecPl$) and the position of the star ($\vecSt$), a vector pointing from the planet's center to the star's center would be, for example,
\begin{equation}
	\vecPtoS = \vecPl - \vecSt.
	\label{eq:pointing from planet to star}
\end{equation}
(the notation $\textcolor{ptoscol}{p\to s}$ is just an easy way to represent that the vector is pointing from the planet to the star)

\begin{center}
	\begin{tikzpicture}[fig with vectors]
		\coordinate (star) at (-2,2);
		\coordinate (planet) at (1,3);
		\draw pic {star={star}{1}{starcol}};
		\draw pic {star={planet}{0.5}{planetcol}};
		\draw[vector={starcol}] (0,0) -- (star) node[midway, below left] {$\vec{r}_{s}$} node[black, above left, xshift=-30pt] {star};
		\draw[vector={planetcol}] (0,0) -- (planet) node[midway, right] {$\vec{r}_{p}$} node[black, above right, xshift=15pt] {planet};
		\draw[dashedvector={ptoscol}] (planet) -- (star) node[midway, above] {$\vec{r}_{p\to s}$};
		\node[below of=o, yshift=15pt] {origin};
	\end{tikzpicture}
\end{center}

However, this vector has a norm equal to the distance between the plant and the star - and we want to use it to calculate the \textit{force} acting on the planet, which has a different magnitude (and units). To do this, we need to be able to scale $\vecPtoS$ to whatever norm we want. Taking inspiration from real numbers, we can employ the following procedure: say we want to scale some non-zero number $x$ to be $y$. We can multiple $x$ by $\frac{y}{x}$, canceling the $x$:
\begin{equation*}
	x \mapsto \cancel{x}\cdot\frac{y}{\cancel{x}} = y.
\end{equation*}
A different way to phrase this is that we simply multiplying (or \textit{scaling}) $x$ by $\frac{1}{x}$, and then multiply the result by $y$:
\begin{equation*}
	x \mapsto \cancel{x}\cdot\frac{1}{\cancel{x}}\cdot y = y.
\end{equation*}

We can apply the same technique to vectors. Scaling a vector by its \textit{reciprocal norm} yields a vector with norm $1$, which we denote by replacing the arrow in the vector notation by a \enquote{hat}:
\begin{equation}
	\uvec{a} = \frac{1}{\norm{\vec{a}}}\vec{a}.
	\label{eq:unit vector}
\end{equation}
This operation is called \newterm{normalization}, and the resulting vector is a \newterm{unit vector} (also: normalized vector).

Now, if we scale a unit vector $\uvec{a}$ by any scalar $\lambda$ we get a vector of the same direction as $\uvec{a}$ (and also $\vec{a}$), and with a norm of $\lambda$. The vectors $\vec{a},\ \uvec{a}$ and $\lambda\uvec{a}$ are all parallel to each other.

Back to our star-planet system: by normalizing the vector $\vecPtoS$ we get a unit vector pointing from the planet to the star: $\textcolor{ptoscol}{\uvecPtoS}$, and scaling by the magnitude of the gravitational attraction (\autoref{eq:gravity_force}) we finally get the correct \textit{gravity force vector}:
\begin{equation}
	\tikz[
		remember picture,
		node distance={15pt}
	]{
		\node[coloredterm={xorange}] (F) {$\vec{F}$};
		\node[right of=F] (eq) {$=$};
		\node[coloredterm={xblue}, right of=eq, xshift=10pt] (scalar) {$G\frac{Mm}{r^{2}}$};
		\node[coloredterm={xgreen}, right of=scalar, xshift=18pt] (vec) {$\uvec{r}_{p\to s}$};

		\node[below of=F, yshift=-15pt] (Ftxt) {vector};
		\node[below of=scalar, yshift=-25pt] (scalartxt) {scalar};
		\node[below of=vec, yshift=-15pt] (vectxt) {vector};

		\draw[coloredarrow={xorange}] (Ftxt.north) -- (F.south);
		\draw[coloredarrow={xblue}] (scalartxt.north) -- (scalar.south);
		\draw[coloredarrow={xgreen}] (vectxt.north) -- (vec.south);
	}
	\label{eq:gravity force vector}
\end{equation}

Indeed, $\vec{F}$ is a vector: it has a magnitude (scalar part) and a direction (vector part).

% TODO: parallel vectors? distributivity of scalar and vector addition: s(a+b) = sa+sb, (x+y)a = xa+ya. Also, just a bit about linear combinations.

% Any expression which is made up of a combination of adding and scaling vectors is called a \newterm{linear combination}.

\subsection{Common Coordinate Systems in 2- and 3-Dimensions} % Cartesian, polar, cylindrical and spherical coordinate systems

\subsection{Geomteric Objects} % Points, lines, planes, intersections

\subsection{Projections and Rejections} % Vector decomposition, projection & rejection, the dot product, the cross product and its issues

\subsection{Vector Spaces} % R2, R3 & Rn and their subsets, linear combinations, span, basis sets, subspaces, the Gram-Schmist process

\subsection{Solving the Motivating Problems}
